#!/usr/bin/env python3

#test out multinomial probability density functionm 

#camel case notation



def mean_cov(matrix): 
    if matrix is 0:
        return -1 #short circuit
    import numpy as np
    npInternalArray = np.array(matrix) #grab the numpy array
    internalMean = np.mean(npInternalArray)


    #retrun(np.cov(matrix))

    #manual calculation of covariant    
    for i in matrix:

        break
    
    return internalMean, 0 #aleph null
